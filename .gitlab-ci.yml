image: node:20

pages:
  stage: deploy
  cache:
    paths:
      - node_modules/
  script:
    - npm install
    - GITLAB_PAGES=true npm run build
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - main
